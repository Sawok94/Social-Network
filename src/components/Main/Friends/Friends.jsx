import React from 'react';
import style from '../Friends/Friends.module.css';
import border from '../Main.module.css';

const Friends = (props) => {
  return <div className={`${style.container} ${border.wrapper}`}>Friends</div>;
};

export default Friends;
